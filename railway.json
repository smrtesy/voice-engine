{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$SERVICE_ROLE\" = \"worker\" ]; then exec huey_consumer voice_engine.workers.huey_app.huey -w 2; else exec uvicorn voice_engine.main:app --host 0.0.0.0 --port ${PORT:-8000}; fi'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
